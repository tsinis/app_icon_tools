import 'dart:typed_data';

import 'package:flutter/material.dart';

const Color _transparent = Color(0x00123456);
Color get transparent => _transparent;

final Uint8List _zeroBytes = Uint8List(0);
Uint8List get zeroBytes => _zeroBytes;
