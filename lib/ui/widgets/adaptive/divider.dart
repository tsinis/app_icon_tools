import 'package:flutter/material.dart';

class AdaptiveDivider extends StatelessWidget {
  const AdaptiveDivider({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Divider(thickness: 0.6, color: Color(0x70808080));
}
